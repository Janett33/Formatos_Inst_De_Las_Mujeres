<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registro de apoyos</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fafafa;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            padding: 25px 0;
        }

        /* REGRESAR */
        .regresar {
            position: absolute;
            top: 20px;
            right: 30px;
            font-size: 16px;
            color: #555;
            text-decoration: underline;
        }

        /* Quitar subrayado de los enlaces */
        a {
            text-decoration: none;
            color: inherit;
        }

        /* GRID DE DESCARGAS */
        .grid-descargas {
            display: grid;
            grid-template-columns: repeat(auto-fit, 200px);
            gap: 40px;
            justify-content: center;
            margin-bottom: 40px;
        }

        /* RECUADROS */
        .banner {
            width: 200px;
            height: 200px;
            background-color: #9b59b6;
            color: white;
            border-radius: 20px;
            transition: transform 0.3s, background-color 0.3s;
            cursor: pointer;

            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 15px;
            box-sizing: border-box;
        }

        .banner:hover {
            background-color: #8e44ad;
            transform: scale(1.05);
        }

        /* Evita el subrayado dentro del banner */
        .banner,
        .banner * {
            text-decoration: none;
            color: white;
        }

        .titulo {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 12px;
            text-align: center;
        }

        .subtitulo {
            margin: 0;
            padding-left: 20px;
            text-align: left;
            font-size: 14px;
            color: white;
        }

        .subtitulo li {
            margin-bottom: 6px;
            color: white;
        }

    </style>
</head>

<body>

    <!-- REGRESAR -->
    <a href="menu_Direccion.html" class="regresar">Regresar</a>

    <h1>Formatos – Dirección</h1>

    <div class="grid-descargas">

        <!-- REGISTRO DE APOYOS -->
        <a href="https://ee.kobotoolbox.org/x/SYdmyYQo" target="_blank">
            <div class="banner">

                <div class="titulo">
                    Registro de Apoyos
                </div>

                <ul class="subtitulo">
                    <li>Apoyos entregados</li>
                    <li>Solicitudes atendidas</li>
                    <li>Solicitudes recibidas</li>
                </ul>

            </div>
        </a>

    </div>

</body>
</html>
