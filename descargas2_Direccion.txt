<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Descargas Dirección</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fafafa;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            padding: 25px 0;
        }

        /* REGRESAR */
        .regresar {
            position: absolute;
            top: 20px;
            right: 30px;
            font-size: 16px;
            color: #555;
            text-decoration: underline;
        }

        /* GRID DE DESCARGAS */
        .grid-descargas {
            display: grid;
            grid-template-columns: repeat(auto-fit, 200px);
            gap: 40px;
            justify-content: center;
            margin-bottom: 40px;
        }

        /* RECUADROS */
        .banner {
            width: 200px;
            height: 200px;
            padding: 15px;

            background-color: #9b59b6;
            color: white;
            font-size: 16px;

            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;

            border-radius: 18px;
            cursor: pointer;
            transition: transform 0.3s, background-color 0.3s;

            overflow: hidden;
        }

        .banner:hover {
            background-color: #8e44ad;
            transform: scale(1.08);
        }

        .titulo {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 6px;
        }

        .subtitulo {
            font-size: 13px;
            line-height: 1.3;
        }

        a {
            text-decoration: none;
            color: inherit;
        }
    </style>
</head>

<body>

    <!-- REGRESAR (corregido sin acentos en archivo) -->
    <a href="menu_Direccion.html" class="regresar">Regresar</a>

    <h1>Formatos – Dirección</h1>

    <div class="grid-descargas">

        <!-- FORMATO COORDINACIÓN -->
        <a href="ESPACIO_EN_BLANCO">
            <div class="banner">
                <div class="titulo">(En espera del formato)</div>
                <div class="subtitulo">
                    N/A
                </div>
            </div>
        </a>

        <!-- ESPACIO PARA FUTURO FORMATO -->
        <div class="banner" style="background-color:#ccc; color:#777; cursor:default;">
            Próximamente
        </div>

    </div>

</body>
</html>
